import React from "react";
import { Button } from "@/components/ui/button";
const DeleteFacility = () => {
  return (
    <Button variant="default" className="bg-primary-600  hover:bg-primary-500">
      Delete
    </Button>
  );
};

export default DeleteFacility;
